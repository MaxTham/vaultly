import { StyleSheet } from 'react-native';

const Typography = StyleSheet.create({
  heading: {
    fontSize: 24,
    fontWeight: 'bold',
  },
  body: {
    fontSize: 16,
    color: '#333',
  },
});

export default Typography;
